/**
 * Takes an array of numbers, and returns the unique numbers. Can you do it in
 * O(N) time?
 *
 * @param array
 * @returns {Object[]}
 */

/// solution

function uniq(array) {
  return array;
}

module.exports = uniq;
