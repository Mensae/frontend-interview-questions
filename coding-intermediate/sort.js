/**
 * Implement the sort function to sort an array of numbers in O(N×log(N)) time.
 *
 * @param {Array} array
 * @returns {Array}
 */

/// solution

function sort(array) {
  return array;
}

module.exports = sort;
